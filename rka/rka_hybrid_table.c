/*
 * rka_hybrid_table.c — oracle-build hybrid dispatch table for Rocket Knight.
 *
 * Empty stub for bring-up: the native (RKARecomp) target does NOT compile this
 * file; only the dev-only RKARecomp_oracle target lists it (see add_recomp_mode
 * in the game-repo CMakeLists). The oracle build isn't wired up yet — this file
 * exists only so CMake's source list for the oracle target resolves. Populate
 * when/if the clown68000 oracle target for RKA is brought online.
 */
